// resources/js/bootstrap.js
// File bootstrap standar untuk Laravel + Vite + Tailwind

// Jika ingin menambah axios atau library lain, tambahkan di sini
// Untuk Tailwind, tidak perlu import di sini, cukup di CSS

// Contoh: inisialisasi axios (opsional, hapus jika tidak perlu)
// import axios from 'axios';
// window.axios = axios;
// window.axios.defaults.headers.common['X-Requested-With'] = 'XMLHttpRequest';

// Kosongkan jika hanya menggunakan Tailwind dan Alpine
